{
  "mcpServers": {
    "canopy": {
      "type": "streamable-http",
      "url": "${CANOPY_MCP_URL:-http://localhost:8787/mcp}",
      "headers": {
        "Authorization": "Bearer ${CANOPY_MCP_TOKEN}"
      }
    }
  }
}
